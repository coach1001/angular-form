import { DynamicAnchorDirective } from './dynamic-anchor.directive';

describe('DynamicAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
